# Git learing record
  ## 2025 12 16
    - using "git init"
  
  ## 2025 12 17
    - git status -----check at any time
    - git add <file name>
    - git commit -m "content"
    - git push 
    - git log     
    -
    - git checkout main  ---switch to the main branch
    - git merge <name>  ---merge hte <name> into the current branch 
    - git branch -M main ---rename the branch to main
    - git branch -b <name> ---crerat a new branch named<>
    - gir branch -d <name> ---delect a branch
  
    -
    .gitignore ----special files with special text form


    -information labels
    - feat:
    - fix:
    - docs:
    - style:
    - refactor:
    - test:
    - chore:
  
  -
  --- .<name> indicates hidden files 